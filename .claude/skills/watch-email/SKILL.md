---
name: watch-email
description: Start the email HTML extractor watcher
allowed-tools: Bash(node:*)
---

Start the watcher script that monitors for Thunder Client JSON responses and auto-opens extracted email HTML in the browser:

```bash
cd api-testing && npm run watch
```
